/*
 * MAX Guard - Защита от несанкционированного удаления
 */

#include <stdio.h>
#include <string.h>

#ifdef _WIN32
#include <windows.h>
#endif

int main() {
    char password[256];
    printf("================================================\n");
    printf("        MAX ANTIVIRUS - ЗАЩИТА УДАЛЕНИЯ\n");
    printf("================================================\n");
    printf("Для продолжения удаления введите пароль администратора: ");

    // В реальном приложении пароль должен храниться в зашифрованном виде в реестре
    // Для примера используем стандартный пароль "MAX2024"
    scanf("%255s", password);

    if (strcmp(password, "MAX2024") == 0) {
        printf("\n[+] Личность подтверждена. Удаление разрешено.\n");
        return 0; // Код успеха для инсталлятора
    } else {
        printf("\n[!] ОШИБКА: Неверный пароль. Доступ запрещен.\n");
#ifdef _WIN32
        MessageBoxA(NULL, "Неверный пароль! Удаление MAX Antivirus отменено.", "MAX Guard", MB_ICONSTOP);
#endif
        return 1; // Код ошибки - прерывает удаление
    }
}
